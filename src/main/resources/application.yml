server:
  error:
    include-message: always
    include-binding-errors: always

spring:
  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html
    cache: false

  datasource:
    url: jdbc:mariadb://localhost:3306/registration
    username: root
    password:

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MariaDBDialect
        format_sql: true
    show-sql: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: vairussyr97@gmail.com
    password: nxmhhdwwiyqqlcpo
    properties:
      mail:
        smtp:
          ssl:
            trust: "*"
          auth: true
          starttls:
            enable: true
            auth: true
          connectiontimeout: 5000
          timeout: 3000
          writetimeout: 5000
